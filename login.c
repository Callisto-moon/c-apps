// c program for login 
#include <stdio.h>
void login(void){
    char Username[20], password[20];
    printf("Enter the Username: ");
    scanf("%s",Username);
    printf("Enter the password: ");
    scanf("%s",password);
}